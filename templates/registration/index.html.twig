{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Nouvel utilisateur{% endblock %}

{% block body %}


<div class="example-wrapper">
    <h1 class="center mb-5">Ajouter un utilisateur</h1>
    <form class="client-form mt-5 p-3" action="{{ path('app_login') }}" method="post">
    {{ form(formRegister)}}
        {#<div class="form-group mb-3">
            <label class="form-label" for="username">Pseudo</label>
            <input class="form-control" type="text" id="username" name="_username">
        </div>
        {{ form_row(form.pseudo, {'label': 'Pseudo' }) }}
        {{ form_row(form.email, {'label': 'Email'}) }}
        {{ form_row(form.password, {'label': 'Mot de passe'}) }}
        {{ form_row(form.roles, {'label': 'Rôle'}) }}
        
        <button class="btn btn-primary w-100 mt-3" type="submit">Enregistrer</button></div>
    </form>
</div>

{% endblock %}
